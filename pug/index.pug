doctype html

html(lang='en')
    head
        script
            | (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
            | (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
            | m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
            | })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
            | ga('create', 'UA-46752372-2', 'auto');
            | ga('send', 'pageview');
        meta(charset='utf-8')
        meta(http-equiv='X-UA-Compatible', content='IE=edge')
        meta(name='viewport', content='width=device-width, initial-scale=1')
        title Evan Dorsky

        script(src='js/zepto.min.js')
        script(src='js/main.js')

        link(href='stylesheets/style.css', rel='stylesheet')

    body
        .container
            .me
                .header
                    h1 Evan Dorsky
                    .email evan.dorsky@icloud.com

                .info
                    ul
                        li Olin College Electrical and Computer Engineering '16
                        li SPG Hardware System Integration Engineer at Apple
                        li San Francisco
                    ul.past
                        li Course designer and teaching team member at Olin College

            .portfolio
                each cat, key in categories
                    .cat(class=key)
                        .photo-box
                            each entry in cat.entries
                                .cat-img(name=entry.name)
                                    if key == "courses"
                                        p #{entry.name}
                                    else
                                        img.color(src='img/'+key+'/'+entry.name+'_tn.jpg')
                                        img.gray(src='img/'+key+'/'+entry.name+'_tn_gray.jpg')
                                .cat-details(name=entry.name)
                                    | Details
                        .cat-title #{cat.title}